import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.perigon.app',
  appName: 'Perigon',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
